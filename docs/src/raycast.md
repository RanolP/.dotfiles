# Raycast

Launcher. Replaces Spotlight.

**Managed by:** Homebrew cask

Spotlight (Cmd+Space, symbolic hotkey ID 64) is disabled via the activation script in `nix/darwin/default.nix`. Cmd+Space opens Raycast instead.
