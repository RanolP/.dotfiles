{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.nix ]] && (cd /Users/ranolp/.dotfiles/nix && nix fmt . 2>/dev/null) || true; } 2>/dev/null || true",
            "statusMessage": "Formatting Nix..."
          }
        ]
      }
    ]
  }
}
