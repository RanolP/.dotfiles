---
name: technical-writing
description: Deprecated — replaced by the docs-write pipeline. Use docs-write (entry), which routes to docs-write-{tutorial,concept,howto,reference,status} and mandates a prose-editor review pass.
---

# technical-writing (deprecated)

This skill's content moved into the `docs-write` pipeline:

- Shared core (information architecture, sentence craft, Korean prose, labels & jargon) → `docs-write/SKILL.md`
- Per-type checklists → `docs-write-tutorial`, `docs-write-concept`, `docs-write-howto`, `docs-write-reference`, `docs-write-status`

This directory is unregistered in `nix/home/default.nix` and kept only as a pointer for repo readers.
