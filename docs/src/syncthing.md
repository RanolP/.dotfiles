# Syncthing

Continuous file sync across devices.

**Managed by:** home-manager service (`services.syncthing.enable = true`)

Runs as a background launchd service. Not a Homebrew cask — the daemon is managed declaratively by home-manager.
